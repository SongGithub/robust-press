#!/usr/bin/env bash

dcr="docker-compose run --rm"

set -e

if [ "$#" -ne 2 ]; then
  echo "Usage: bin/deploy_cfn <component_name> <env>"
  exit 1
fi

component_name=$1
env=$2

$dcr kt clean && $dcr kt deploy -t cfn -c "$1" -e default -e "$2"